#start mysql
sudo mysqld -u root

#stop mysql
sudo mysqladmin shutdown

# 
mysql -uroot


 CREATE USER 'tracklocation'@'localhost' IDENTIFIED BY 'tracklocation123';
 
 GRANT ALL PRIVILEGES ON *.* TO 'tracklocation'@'localhost'   WITH GRANT OPTION;
 
 
 
 It is not safe to rely on the system's timezone settings
 php.ini
 date.timezone=PRC
 
 
 
 
 